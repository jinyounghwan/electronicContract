<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.samsung.framework.mapper.log.RestApiMapper">
    <insert id="insertApiAccessLog" useGeneratedKeys="true">
        INSERT INTO PROCESS_LOG(LOG_SEQ, REF_NO, LOG_TYPE, PROCESS_STEP, IP_ADDRESS, MAC_ADDRESS, CREATED_BY, CREATED_AT)
        VALUES (#{logSeq}, #{refNo}, #{logType}, #{processStep}, #{ipAddress}, #{macAddress}, #{createdBy}, Sysdate())
        <selectKey keyProperty="logSeq" resultType="long">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <update id="updateApiAccessLog">
        UPDATE PROCESS_LOG SET LOG_TYPE = #{logType}, CREATED_AT = sysdate() WHERE LOG_SEQ = #{logSeq}
    </update>
</mapper>